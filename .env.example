# Copy this file to .env for local secrets. Do not commit the .env.

# OpenAI is disabled by default. Set this only if you explicitly enable
# external embeddings or LLM features in config/config.yaml.
OPENAI_API_KEY=

# Optional override if running GROBID locally.
GROBID_URL=http://localhost:8070
